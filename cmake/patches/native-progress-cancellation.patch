--- a/include/stable-diffusion.h
+++ b/include/stable-diffusion.h
@@ -443,11 +443,17 @@
 
 typedef void (*sd_log_cb_t)(enum sd_log_level_t level, const char* text, void* data);
 typedef void (*sd_progress_cb_t)(int step, int steps, float time, void* data);
+// iiLocalDiffusion: preserve the legacy callback and expose unambiguous phases.
+enum sd_progress_stage_t { SD_PROGRESS_LOAD, SD_PROGRESS_SAMPLE, SD_PROGRESS_DECODE };
+typedef void (*sd_progress_stage_cb_t)(enum sd_progress_stage_t stage, int step, int steps, float time, void* data);
+typedef bool (*sd_abort_cb_t)(void* data);
 typedef void (*sd_preview_cb_t)(int step, int frame_count, sd_image_t* frames, bool is_noisy, void* data);
 typedef bool (*sd_graph_eval_callback_t)(struct ggml_tensor* t, bool ask, void* user_data);
 
 SD_API void sd_set_log_callback(sd_log_cb_t sd_log_cb, void* data);
 SD_API void sd_set_progress_callback(sd_progress_cb_t cb, void* data);
+SD_API void sd_set_progress_stage_callback(sd_progress_stage_cb_t cb, void* data);
+SD_API void sd_set_abort_callback(sd_abort_cb_t cb, void* data);
 // In each sampling pass, a positive interval previews every Nth denoiser step, while a
 // negative interval previews only completed logical step -interval. Zero previews the final
 // completed step of the first sampling pass (base-resolution or high-noise).
--- a/src/core/util.h
+++ b/src/core/util.h
@@ -84,6 +84,9 @@
 bool parse_strict_bool(const std::string& text, bool& value);
 
 void pretty_progress(int step, int steps, float time);
+void pretty_progress_stage(sd_progress_stage_t stage, int step, int steps, float time);
+bool sd_should_abort();
+void sd_check_abort();
 void pretty_bytes_progress(int step, int steps, uint64_t bytes_processed, float elapsed_seconds);
 
 void log_printf(sd_log_level_t level, const char* file, int line, const char* format, ...);
--- a/src/core/util.cpp
+++ b/src/core/util.cpp
@@ -1,3 +1,4 @@
+#include <stdexcept>
 #include "core/util.h"
 #include <algorithm>
 #include <cctype>
@@ -340,6 +341,10 @@
     return n_threads > 0 ? (n_threads <= 4 ? n_threads : n_threads / 2) : 4;
 }
 
+static sd_progress_stage_cb_t sd_progress_stage_cb = nullptr;
+static void* sd_progress_stage_data = nullptr;
+static sd_abort_cb_t sd_abort_cb = nullptr;
+static void* sd_abort_data = nullptr;
 static sd_progress_cb_t sd_progress_cb = nullptr;
 void* sd_progress_cb_data              = nullptr;
 
@@ -535,6 +540,11 @@
 }
 
 void pretty_progress(int step, int steps, float time) {
+    pretty_progress_stage(SD_PROGRESS_DECODE, step, steps, time);
+}
+
+void pretty_progress_stage(sd_progress_stage_t stage, int step, int steps, float time) {
+    if (sd_progress_stage_cb) sd_progress_stage_cb(stage, step, steps, time, sd_progress_stage_data);
     if (sd_progress_cb) {
         sd_progress_cb(step, steps, time, sd_progress_cb_data);
         return;
@@ -548,10 +558,12 @@
         speed = 1.0f / speed;
         unit  = "it/s";
     }
-    print_progress_line(step, steps, sd_format("%.2f%s", speed, unit));
+    if (!sd_progress_stage_cb) print_progress_line(step, steps, sd_format("%.2f%s", speed, unit));
 }
 
 void pretty_bytes_progress(int step, int steps, uint64_t bytes_processed, float elapsed_seconds) {
+    if (sd_progress_stage_cb) sd_progress_stage_cb(SD_PROGRESS_LOAD, step, steps,
+        elapsed_seconds / (step + 1e-6f), sd_progress_stage_data);
     if (sd_progress_cb) {
         float time = elapsed_seconds / (step + 1e-6f);
         sd_progress_cb(step, steps, time, sd_progress_cb_data);
@@ -561,6 +573,7 @@
         return;
     }
 
+    if (sd_progress_stage_cb) return;
     double bytes_per_second = 0.0;
     if (elapsed_seconds > 0.0f) {
         bytes_per_second = bytes_processed / (double)elapsed_seconds;
@@ -645,6 +658,20 @@
 void sd_set_progress_callback(sd_progress_cb_t cb, void* data) {
     sd_progress_cb      = cb;
     sd_progress_cb_data = data;
+}
+void sd_set_progress_stage_callback(sd_progress_stage_cb_t cb, void* data) {
+    sd_progress_stage_cb = cb;
+    sd_progress_stage_data = data;
+}
+void sd_set_abort_callback(sd_abort_cb_t cb, void* data) {
+    sd_abort_cb = cb;
+    sd_abort_data = data;
+}
+void sd_check_abort() {
+    if (sd_should_abort()) throw std::runtime_error("Native image generation interrupted.");
+}
+bool sd_should_abort() {
+    return sd_abort_cb && sd_abort_cb(sd_abort_data);
 }
 void sd_set_preview_callback(sd_preview_cb_t cb, preview_t mode, int interval, bool denoised, bool noisy, void* data) {
     sd_preview_cb       = cb;
--- a/src/stable-diffusion.cpp
+++ b/src/stable-diffusion.cpp
@@ -2490,7 +2490,7 @@
             float step_seconds = last_progress_us != nullptr && *last_progress_us > 0
                                      ? (now - *last_progress_us) / 1000000.f
                                      : 0.f;
-            pretty_progress(showstep, (int)total_steps, step_seconds);
+            pretty_progress_stage(SD_PROGRESS_SAMPLE, showstep, (int)total_steps, step_seconds);
             if (last_progress_us != nullptr) {
                 *last_progress_us = now;
             }
@@ -2651,7 +2651,7 @@
             }
 
             if (step == 1 || step == -1) {
-                pretty_progress(0, (int)steps, 0);
+                pretty_progress_stage(SD_PROGRESS_SAMPLE, 0, (int)steps, 0);
                 last_progress_us = ggml_time_us();
             }
 
@@ -3878,11 +3878,16 @@
         return nullptr;
     }
 
-    if (!sd_ctx->sd->init(sd_ctx_params)) {
+    try {
+        if (!sd_ctx->sd->init(sd_ctx_params)) {
+            delete sd_ctx->sd;
+            free(sd_ctx);
+            return nullptr;
+        }
+    } catch (...) {
         delete sd_ctx->sd;
-        sd_ctx->sd = nullptr;
         free(sd_ctx);
-        return nullptr;
+        throw;
     }
     return sd_ctx;
 }
--- a/src/model_loader.cpp
+++ b/src/model_loader.cpp
@@ -1022,6 +1022,7 @@
                 std::vector<uint8_t> zip_entry_buffer;
 
                 while (true) {
+                    if (sd_should_abort()) { failed = true; break; }
                     int64_t t0, t1;
                     size_t idx = tensor_idx.fetch_add(1);
                     if (idx >= tensors_to_process.size() || failed) {
@@ -1216,6 +1217,9 @@
         for (auto& w : workers) {
             w.join();
         }
+        // Unwind only on the caller thread, after all tensor readers joined.
+        // Returning empty tensors here reaches assertions in some conditioners.
+        sd_check_abort();
 
         if (failed) {
             success = false;
--- a/src/core/ggml_runner.cpp
+++ b/src/core/ggml_runner.cpp
@@ -721,6 +721,7 @@
 }
 
 bool GGMLRunner::execute_segment(ggml_cgraph* graph, int n_threads) {
+    sd_check_abort();
     if (sd_backend_is_cpu(runtime_backend)) {
         sd_backend_cpu_set_n_threads(runtime_backend, n_threads);
     }
@@ -741,6 +742,7 @@
                                                              sd_get_backend_eval_callback_data());
     }
     workspace_.synchronize();
+    sd_check_abort();
     if (status != GGML_STATUS_SUCCESS) {
         LOG_ERROR("%s compute failed: %s", get_desc().c_str(), ggml_status_to_string(status));
         return false;
